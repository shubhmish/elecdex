image:
  file: .gitpod.Dockerfile

tasks:
  - init: npm install
  - command: npx electron .

ports:
  - port: 3000
    onOpen: open-preview

vscode:
  extensions:
    - dbaeumer.vscode-eslint
    - esbenp.prettier-vscode
